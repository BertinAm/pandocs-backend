# Core
Django==4.2.11
djangorestframework==3.15.1
django-cors-headers==4.3.1
whitenoise==6.6.0
python-dotenv==1.0.1
dj-database-url==3.1.2
psycopg2-binary==2.9.11
gunicorn==23.0.0

# WebSockets (install separately if Redis is available)
# channels==4.x
# channels-redis==4.x
# daphne==4.x

# PDF generation (optional — endpoint returns HTML if not installed)
# xhtml2pdf==0.2.x

# Background tasks (optional)
# celery==5.x
# redis==5.x
